# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
MAKEOPTS="-j12 -l12"
PORTAGE_NICENESS=19
EMERGE_DEFAULT_OPTS="--jobs=12 --load-average=12"
FEATURES="binpkg-logs buildpkg cgroup collision-protect downgrade-backup ipc-sandbox network-sandbox parallel-install sign"
GRUB_PLATFORMS="efi-64"
ACCEPT_LICENSE="*"
USE="X iosevka ubuntu -pax-kernel -additions -doc -extensions -headless -java opengl -pam -sdk -vboxwebsrv upower libnotify fontconfig polkit truetype jpeg png ffmpeg xorg consolekit elogind pulseaudio alsa pavucontrol wifi networkmanager ipv6 fonts wayland ABI_X86=(64) udev"
INPUT_DEVICES="libinput"
ACCEPT_KEYWORDS="~amd64"
VIDEO_CARDS="nvidia"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

GENTOO_MIRRORS="http://www.gtlib.gatech.edu/pub/gentoo rsync://rsync.gtlib.gatech.edu/gentoo https://gentoo.osuosl.org/ http://gentoo.osuosl.org/ http://gentoo.mirrors.pair.com/ https://mirrors.rit.edu/gentoo/ http://mirrors.rit.edu/gentoo/ ftp://mirrors.rit.edu/gentoo/ rsync://mirrors.rit.edu/gentoo/ http://gentoo.mirrors.tds.net/gentoo"
